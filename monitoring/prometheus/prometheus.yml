global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: stellar-goal-vault-backend
    metrics_path: /metrics
    static_configs:
      - targets:
          - backend:3001
